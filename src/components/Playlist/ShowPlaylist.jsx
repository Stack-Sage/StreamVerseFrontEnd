import { buttonStyle } from '@/styles/globals'
import React from 'react'

const ShowPlaylist = () => {
  return (
    <>
    
    <div>``
      <button className= { `text-xl cursor-pointer ${buttonStyle} hue-rotate-60  px-3 py-2 rounded-full  `}>
         Create Playlist
      </button>
    </div>
    </>
  )
}

export default ShowPlaylist